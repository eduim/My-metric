{
  "name": "My-metric",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "setup": "npm run dev:docker && cd apps/server && npm run dev:setup",
    "dev": "npm run dev:docker && turbo run dev --parallel",
    "dev:docker": "docker-compose up -d"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "npm@8.11.0"
}
